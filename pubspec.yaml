name: dapp
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_auth: ^4.6.1
  geolocator: ^9.0.2
  firebase_core: ^2.13.0
  cloud_firestore: ^4.7.1
  get: ^4.6.5  # For managing state
  geocoding: ^2.1.0 # To get address from latlng  
  get_storage: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
        - assets/